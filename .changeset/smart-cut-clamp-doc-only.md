---
---

`@nodaro/shared`: documentation only — `smart-cut-windows.ts` now states that
its bound and clamp serve BOTH smart-cut nodes (generate-video-pro and
combine-videos) and why an `<input max>` is not a substitute for the clamp. No
exported value, type or behavior changes, so nothing to release.
