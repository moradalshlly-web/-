---
"@nodaro/shared": minor
---

Add the measured video output-canvas table and the start/end frame fit math: `VIDEO_OUTPUT_CANVAS` / `resolveOutputCanvas` (real pixel sizes per model, resolution and ratio, harvested from production renders), plus `computeFrameFitPlan`, `resolveFrameFitAspect` and `resolveFrameDelivery` with the `FrameFit` / `FrameDelivery` vocabularies.
