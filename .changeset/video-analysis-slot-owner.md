---
"@nodaro/shared": minor
---

`entitySlotSchema` gains an optional `owner: { slotId, relation }` — whose an object slot is (worn by, held by, driven by a cast person or creature), so a shirt described on the man and wired as its own slot is one shirt, not two.
