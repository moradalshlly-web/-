---
"@nodaro/shared": minor
---

New `PROJECTED_TRIGGER_NODE_TYPES` (with `SCHEDULE_TRIGGER_NODE_TYPE`, `WEBHOOK_TRIGGER_NODE_TYPE` and `isProjectedTriggerNodeType`): the node types a Nodaro server projects onto real trigger rows when a workflow is saved. Additive.
