---
"@nodaro/shared": patch
---

Captions: an unset `look` on the static `subtitle` style now resolves the `clean` preset (`DEFAULT_SUBTITLE_LOOK`) — a pinned sans — instead of no face at all, which rendered as the browser-default serif once a subtitle routed to Remotion.
