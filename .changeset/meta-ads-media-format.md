---
"@nodaro/shared": minor
---

Meta Ads: creative-format vocabulary (`META_ADS_FORMATS`, `classifyCreativeFormat`), featured-ad output helpers (`featuredMetaAdOutputs`, `clampMetaAdsFeaturedIndex`), and the node's typed `text` / `image` / `video` output handles resolve as the canonical single-media producers in `resolveEffectiveSourceType`.
