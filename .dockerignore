node_modules
.next
dist

.git
.gitignore
.claude
.cursor

docs
supabase
# Self-host migration runner needs the SQL files inside the image
# (applied on boot when RUN_MIGRATIONS_ON_BOOT=true — see start.sh).
!supabase/migrations
!supabase/migrations/*.sql

.env
.env.*
!.env.example

docker-compose*
Dockerfile
.dockerignore

*.md
!backend/skills/**/*.md
LICENSE

# Local session state and reports must not enter the build context.
.superpowers
.remember
.agents
.codex
**/coverage
**/playwright-report
**/test-results

# CI runs and typechecks these before release. They are not runtime inputs;
# test-only edits should not invalidate application compilation layers.
**/*.test.ts
**/*.test.tsx
**/*.test.mjs
**/*.spec.ts
**/*.spec.tsx
